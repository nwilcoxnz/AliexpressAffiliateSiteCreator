import * as config from "react-global-configuration";
 
config.set({ 
    categories: ['Fashion', 'Gadgets', 'Home', 'Stationery'],
    siteTitle: 'Site Title',
    tagline: 'Quality products from Aliexpress'
});